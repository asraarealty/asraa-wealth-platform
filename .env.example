# Base URL of the FastAPI backend.
# Leave empty (or omit) to use same-origin requests (e.g. when Next.js and
# the API are served from the same host via a reverse-proxy).
# Example for local development: NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=
